# Moved → [zap-proto/syntax](https://github.com/zap-proto/syntax)

This repository has moved to **`github.com/zap-proto/syntax`** as part of
the `zap-protocol` → `zap-proto` org consolidation.

GitHub maintains automatic redirects on the old path:
- `git clone github.com/zap-protocol/syntax.git` still works
- Existing Go module imports of `github.com/zap-protocol/syntax` still resolve

For new code, use the short path:

    import "github.com/zap-proto/syntax"

The `zap-protocol` org is preserved for site continuity at
[zap-protocol.github.io](https://zap-protocol.github.io); active development
lives at [zap-proto.io](https://zap-proto.io).
